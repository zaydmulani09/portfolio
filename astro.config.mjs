// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  output: 'static',

  // Deployment placeholders — fill in at deploy time (e.g. GitHub Pages).
  // site: 'https://zaydmulani09.github.io',
  // base: '/portfolio',
});
